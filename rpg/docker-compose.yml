version: '3.8'

services:
  battle-service:
    build: .
    container_name: rpg-battle-simulator
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development